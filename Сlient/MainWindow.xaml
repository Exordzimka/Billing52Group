<Window x:Class="Сlient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Сlient"
        mc:Ignorable="d"
        Title="MainWindow" Height="700" Width="1000">
    <Grid x:Name="LayoutRoot" Background="White">
        <StackPanel>
            <Menu Height="20" VerticalAlignment="Top">
                <MenuItem Header="Договора">
                    <MenuItem Header="Открыть договора" ></MenuItem>
                    <MenuItem Header="Open Project" >
                        <MenuItem Header="WinForms"></MenuItem>
                        <MenuItem Header="WPF" ></MenuItem>
                    </MenuItem>
                    <Separator />
                    <MenuItem Header="Exit" ></MenuItem>
                </MenuItem>
                <MenuItem Header="Тарифы" ></MenuItem>
                <MenuItem Header="Услуги" ></MenuItem>
            </Menu>
            <ToolBar Height="25" VerticalAlignment="Top">
                    <Separator />
                <Button>иконка+</Button>
                    <Separator />
                <Button>иконкаред</Button>
                    <Separator />
                <Button>инонка-</Button>
            </ToolBar>
        </StackPanel>
    </Grid>
</Window>
